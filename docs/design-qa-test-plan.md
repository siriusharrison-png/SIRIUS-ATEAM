# Design QA CLI 测试计划

> 测试日期：2026-02-11
> 测试人员：转转

---

## 一、测试目标

验证 CLI 工具能否正确检测代码中的样式问题

---

## 二、测试准备

### 需要的东西
- [x] CLI 工具代码（已在 `~/Desktop/design-qa-cli/`）
- [x] Tokens 文件（已有 `design-tokens.json`）
- [ ] 测试用的代码文件（下面会创建）

### 运行命令
```bash
cd ~/Desktop/design-qa-cli
node bin/cli.js check ./test --tokens ./design-tokens.json
```

---

## 三、测试用例

### 用例 1：颜色检测

| 编号 | 场景 | 输入 | 预期结果 |
|------|------|------|----------|
| 1.1 | 硬编码品牌色 | `color: #1161fe` | ❌ 报错，提示用 var(--brand-0) |
| 1.2 | 硬编码文字色 | `color: #181818` | ❌ 报错，提示用 var(--text-dark-1) |
| 1.3 | 使用 CSS 变量 | `color: var(--brand-0)` | ✅ 通过，不报错 |
| 1.4 | 不在 tokens 里的颜色 | `color: #ff00ff` | ✅ 通过（不在规范里，不管） |

### 用例 2：间距检测

| 编号 | 场景 | 输入 | 预期结果 |
|------|------|------|----------|
| 2.1 | 规范内的间距 | `padding: 16px` | ✅ 通过 |
| 2.2 | 不规范的间距 | `padding: 17px` | ❌ 报错，建议改成 16px |
| 2.3 | 不规范的间距 | `margin: 15px` | ❌ 报错，建议改成 16px 或 14px |
| 2.4 | 边框宽度 | `border: 1px solid` | ✅ 通过，不算间距 |

### 用例 3：文件类型支持

| 编号 | 文件类型 | 预期结果 |
|------|----------|----------|
| 3.1 | `.css` | ✅ 能扫描 |
| 3.2 | `.tsx` | ✅ 能扫描 |
| 3.3 | `.vue` | ✅ 能扫描 |
| 3.4 | `.md` | ✅ 跳过，不扫描 |

### 用例 4：自定义 Tokens

| 编号 | 场景 | 预期结果 |
|------|------|----------|
| 4.1 | 使用默认 tokens | ✅ 正常运行 |
| 4.2 | 指定自己的 tokens 文件 | ✅ 正常运行 |
| 4.3 | tokens 文件不存在 | ❌ 报错提示 |

---

## 四、测试步骤

### Step 1：基础功能测试
```bash
# 进入项目目录
cd ~/Desktop/design-qa-cli

# 运行检测
node bin/cli.js check ./test --tokens ./design-tokens.json
```
**检查点**：能看到检测结果输出

### Step 2：创建更多测试文件
在 `~/Desktop/design-qa-cli/test/` 目录下创建不同场景的测试文件

### Step 3：验证每个用例
逐个运行，对照上面的表格检查结果

### Step 4：记录问题
发现的 bug 或建议记录在下方

---

## 五、测试结果记录

| 用例编号 | 结果 | 备注 |
|----------|------|------|
| 1.1 | ✅ | #1161fe 被检测，建议用 token |
| 1.2 | ✅ | #181818 被检测，建议用 token |
| 1.3 | ✅ | var(--brand-0) 不报错 |
| 1.4 | ✅ | #ff00ff 不在 tokens 里，正确忽略 |
| 2.1 | ✅ | 16px、24px、32px 不报错 |
| 2.2 | ✅ | 17px 报错，建议 16px |
| 2.3 | ✅ | 15px 报错，建议 14px |
| 2.4 | ✅ | border: 1px 正确忽略 |
| 3.1 | ✅ | Card.css 正常扫描 |
| 3.2 | ✅ | Button.tsx 正常扫描 |
| 3.3 | ✅ | Component.vue 正常扫描 |
| 3.4 | ✅ | README.md 被跳过 |
| 4.1 | ✅ | 使用默认 tokens 正常 |
| 4.2 | ✅ | 指定自定义 tokens 正常 |
| 4.3 | ✅ | 文件不存在时正确报错 |

**图例**：✅ 通过 | ❌ 失败 | ⬜ 未测试

---

## 六、发现的问题

| 编号 | 问题描述 | 严重程度 | 状态 |
|------|----------|----------|------|
| | | | |

---

## 七、测试总结

测试完成！

- 总用例数：15
- 通过：15
- 失败：0
- 通过率：**100%**

CLI 工具基础功能全部正常。

---

*测试计划 by Design QA Agent · 2026.02.10*
